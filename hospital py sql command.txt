-- Create the database
CREATE DATABASE IF NOT EXISTS hospital;
USE hospital;

-- Create the patient table
CREATE TABLE IF NOT EXISTS patient (
    pid INT PRIMARY KEY,
    pname VARCHAR(30),
    age INT,
    disease VARCHAR(50)
);
